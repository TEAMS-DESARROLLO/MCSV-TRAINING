server:
    port: 9004
spring:
    application:
        name: mcsv-training
    config:
        import: ${CONFIG_SERVER} 

    cloud:
        config:
            #nombre base que se busca en el git o repositorio
            name: config-server    

